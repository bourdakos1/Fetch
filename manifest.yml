declared-services:
  dialog-service:
    label: dialog
    plan: standard
applications:
- services:
  - dialog-service
  name: fetch-test
  command: node app.js
  path: .
  memory: 512M
  env:
    NODE_ENV: production
  host: fetch-test
